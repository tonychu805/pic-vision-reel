# Same minimal Netlify config as pic-vision-cloud-console -- the Next.js
# Runtime plugin handles the App Router (dynamic /r/[reelId] pages) as
# Netlify Functions automatically.
[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
